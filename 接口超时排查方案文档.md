---
share: true
---

# 基础环境信息采集

## 系统版本
```bash

# 包含操作系统标识数据的配置文件
cat /etc/os-release

# 打印当前系统的内核和硬件架构信息
uname -a

```

## CPU 总数
```bash
lscpu
```

## 总内存

```bash
free -h
```
举例
```text
free -h
              total        used        free      shared  buff/cache   available
Mem:            15G        9.6G        206M         91M        5.9G        5.7G
Swap:          7.9G        174M        7.7G
```

#### 参数解析

| **参数**         | **含义**     | **架构师视角**                                                  |
| -------------- | ---------- | ---------------------------------------------------------- |
| **total**      | 物理内存总大小    | 系统识别到的总 RAM，本例为 15G。                                       |
| **used**       | 已使用的内存     | **used = total - free - buff/cache**。这是被进程实时占用的部分。         |
| **free**       | 未被分配的内存    | 纯粹处于闲置状态的内存。**注意：** Linux 的哲学是“内存不用白不用”，所以这个值通常很小。         |
| **shared**     | 共享内存       | 主要用于进程间通信（如 tmpfs）。                                        |
| **buff/cache** | 缓存/缓冲      | **关键指标。** Linux 会将暂时不用的空闲内存用于缓存文件 IO 数据，以提升性能。             |
| **available**  | **实际可用内存** | **评估健康度最重要的指标。** 它表示在不触发 Swap（交换分区）的前提下，系统还能为新进程提供的最大内存估值。 |
| **Swap**       | 交换分区       | 硬盘上的虚拟内存。当物理内存（Mem）严重不足时，系统会将冷数据移到此处。                      |

### 如何评估系统是否正常

在 Linux 运维中，评价“内存是否够用”不能只看 `free`，而要看 **Available** 和 **Swap 的活跃度**。

#### 核心评估公式

$$ \text{内存真实占用率} = \frac{\text{total} - \text{available}}{\text{total}} \times 100\% $$

- **本例计算：** $(15 - 5.7) / 15 \approx 62\%$。
    
- **结论：** 内存尚有余量，属于**健康状态**。

#### 检查指标清单

- **看 `available` 而非 `free`：** 只要 `available` 充足（通常 > 20%），即便 `free` 只有几百 MB，系统运行也是极快的。本例中 `available` 为 5.7G，非常安全。
    
- **看 `Swap` 的使用情况：**
    
    - `used` 为 174M，相对 7.9G 的总量非常小，说明曾有过内存压力，但目前已缓解。
        
    - **预警：** 如果 `Swap` 的 `used` 持续增长，或者物理内存 `available` 趋近于 0，说明物理内存已耗尽，系统会出现明显的 IO 抖动（磁盘交换）。
        
- **看 `buff/cache` 的占比：** 如果 `buff/cache` 很大（如本例 5.9G），说明系统 IO 效率良好。当新程序启动需要内存时，内核会自动回收这部分空间。

#### Java 架构优化建议

目前服务器状态稳定。建议从以下几点防患于未然：

1. **JVM 堆内存配置：** 当前已用 9.6G。请检查正在运行的 Java 进程，确保 `-Xms` 和 `-Xmx` 设置合理。建议 `Xmx` 总和不要超过 `total` 的 70%~80%，留出空间给 `buff/cache` 和系统内核。
    
2. **排查 Swap 使用：** 虽然目前 174M 影响不大，但建议通过 `vmstat 1` 查看 `si` (swap in) 和 `so` (swap out) 是否频繁。如果 si/so 持续非 0，即使 `available` 还有，也说明存在内存争抢，需检查 `swappiness` 内核参数（建议设为 10）。

## 磁盘

```bash
df -h
```
举例
```text
文件系统                 容量    已用   可用 已用% 挂载点
devtmpfs                 7.9G     0  7.9G    0% /dev
tmpfs                    7.9G     0  7.9G    0% /dev/shm
tmpfs                    7.9G   58M  7.8G    1% /run
tmpfs                    7.9G     0  7.9G    0% /sys/fs/cgroup
/dev/mapper/centos-root   50G  4.0G   46G    8% /
/dev/sda1               1014M  137M  878M   14% /boot
/dev/mapper/centos-home  241G   17G  225G    7% /home
tmpfs                    1.6G     0  1.6G    0% /run/user/0
overlay                  241G   17G  225G    7% /home/workserver/work-docker-data/docker/overlay2/50f5117999a88ddbc1cbf2d9baaa8affe06682ce0720a89f8e16e34af11f7af9/merged
overlay                  241G   17G  225G    7% /home/workserver/work-docker-data/docker/overlay2/5a7f744d49a0cd65611ef960e294fa9c5b427da2f59cc14ea5b96cc6c7cedb89/merged
```
### 参数解析

| **参数**                | **含义**              | **架构师视角**                                |
| --------------------- | ------------------- | ---------------------------------------- |
| **文件系统 (Filesystem)** | 磁盘分区或逻辑卷的名称。        | 关注是否为 LVM（如 `/dev/mapper/...`），便于后期在线扩容。 |
| **容量 (Size)**         | 该分区的总存储空间。          | 评估硬件资源总量。                                |
| **已用 (Used)**         | 已经使用的存储空间。          | 结合业务增长曲线，预判风险。                           |
| **可用 (Avail)**        | 剩余可用的存储空间。          | 系统运行的“安全垫”，太小会导致应用无法写日志或崩溃。              |
| **已用% (Use%)**        | 已用空间占总空间的百分比。       | **监控报警的核心指标**，通常超过 80% 需要介入。             |
| **挂载点 (Mounted on)**  | 该分区在 Linux 目录树中的位置。 | 决定了哪些路径的数据会消耗该分区的空间。                     |
